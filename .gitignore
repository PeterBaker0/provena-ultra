# deps
node_modules/
.pnpm-store/

# builds
dist/
build/
*.tsbuildinfo
.turbo/

# env
.env
.env.local
.env.*.local

# logs / misc
*.log
coverage/
.DS_Store

# generated keycloak realm (from prepare-realm script)
# the directory itself is committed (.gitkeep) so Docker bind mounts never
# create it root-owned before the prepare-realm script runs
docker/keycloak/import/*.json

# local data volumes
.data/
