{
  "name": "@provena/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint configured for @provena/worker'",
    "test": "echo 'No tests for @provena/worker'"
  },
  "dependencies": {
    "@provena/config": "workspace:*",
    "@provena/db": "workspace:*",
    "@provena/observability": "workspace:*",
    "@provena/queue": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
