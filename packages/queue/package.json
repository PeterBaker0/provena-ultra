{
  "name": "@provena/queue",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint configured for @provena/queue'",
    "test": "echo 'No tests for @provena/queue'"
  },
  "dependencies": {
    "@provena/config": "workspace:*",
    "@provena/observability": "workspace:*",
    "pg-boss": "^11.1.1"
  }
}
